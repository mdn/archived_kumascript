<%
    log.debug('page.subpages: ' + kuma.inspect(page.subpages('en-US', 'HTML')));
    log.debug("page: " + kuma.inspect(page));
    /*
    log.debug("page.subpages: " + JSON.stringify(page.subpages));
    var subpages = page.subpages('en-US', 'HTML');
    */
%>
