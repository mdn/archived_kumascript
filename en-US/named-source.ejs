<span lang='de' class='lang lang-de'><span class="comment">We can't use the 'source' template for this because Mediawiki can't do stuff based on the presence or absence of a parameter.  See the following link and search for 'unfortunately': http://meta.wikimedia.org/wiki/Help:Template_names%2C_variable_names_and_parameters_depending_on_a_variable_or_parameter#A_parameter_name_depending_on_a_parameter</span>{{mediawiki.external('http://lxr.mozilla.org/mozilla/source/' + $0 + ' ' + $1)}}
</span><span lang='es' class='lang lang-es'><span class="comment">We can't use the 'source' template for this because Mediawiki can't do stuff based on the presence or absence of a parameter.  See the following link and search for 'unfortunately': http://meta.wikimedia.org/wiki/Help:Template_names%2C_variable_names_and_parameters_depending_on_a_variable_or_parameter#A_parameter_name_depending_on_a_parameter</span>{{mediawiki.external('http://lxr.mozilla.org/mozilla/source/' + $0 + ' ' + $1)}}
</span><span lang='fr' class='lang lang-fr'><span class="comment">We can't use the 'source' template for this because Mediawiki can't do stuff based on the presence or absence of a parameter.  See the following link and search for 'unfortunately': http://meta.wikimedia.org/wiki/Help:Template_names%2C_variable_names_and_parameters_depending_on_a_variable_or_parameter#A_parameter_name_depending_on_a_parameter</span>{{mediawiki.external('http://lxr.mozilla.org/mozilla/source/' + $0 + ' ' + $1)}}
</span><span lang='it' class='lang lang-it'>{{mediawiki.external('http://lxr.mozilla.org/mozilla/source/' + $0 + ' ' + $1)}}

<div class="noinclude">
</div>
{{ wiki.languages( { "en": "Template:Named-source" } ) }}</span><span lang='ja' class='lang lang-ja'><span class="comment">We can't use the 'source' template for this because Mediawiki can't do stuff based on the presence or absence of a parameter.  See the following link and search for 'unfortunately': http://meta.wikimedia.org/wiki/Help:Template_names%2C_variable_names_and_parameters_depending_on_a_variable_or_parameter#A_parameter_name_depending_on_a_parameter</span>{{mediawiki.external('http://lxr.mozilla.org/mozilla/source/' + $0 + ' ' + $1)}}
</span><span lang='ko' class='lang lang-ko'><span class="comment">We can't use the 'source' template for this because Mediawiki can't do stuff based on the presence or absence of a parameter.  See the following link and search for 'unfortunately': http://meta.wikimedia.org/wiki/Help:Template_names%2C_variable_names_and_parameters_depending_on_a_variable_or_parameter#A_parameter_name_depending_on_a_parameter</span>{{mediawiki.external('http://lxr.mozilla.org/mozilla/source/' + $0 + ' ' + $1)}}
</span><span lang='pl' class='lang lang-pl'><span class="comment">We can't use the 'source' template for this because Mediawiki can't do stuff based on the presence or absence of a parameter.  See the following link and search for 'unfortunately': http://meta.wikimedia.org/wiki/Help:Template_names%2C_variable_names_and_parameters_depending_on_a_variable_or_parameter#A_parameter_name_depending_on_a_parameter</span>{{mediawiki.external('http://lxr.mozilla.org/mozilla/source/' + $0 + ' ' + $1)}}
</span><span lang='zh-cn' class='lang lang-zh-cn'><p><span class="comment">We can't use the 'source' template for this because Mediawiki can't do stuff based on the presence or absence of a parameter.  See the following link and search for 'unfortunately': http://meta.wikimedia.org/wiki/Help:Template_names%2C_variable_names_and_parameters_depending_on_a_variable_or_parameter#A_parameter_name_depending_on_a_parameter</span>{{mediawiki.external('http://lxr.mozilla.org/mozilla/source/' + $0 + ' ' + $1)}}
</p><p><span class="comment">Copied from en</span>
</p></span>
