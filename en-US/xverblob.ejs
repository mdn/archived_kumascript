
<pre class="script">
var ar = string.split($0, ".");
var len = #ar;

var b = 100000 * ar[0];	// First component is always *100000

if (len > 1) {
  b = b + (ar[1] * 10000); // second component, if present, is always *10000
}
if (len > 2) {
  b = b + (ar[2] * 100); // third component, if present, is always *100
}
if (len > 3) {
  b = b + ar[3];  // fourth comonent isn't multiplied
}
(b); 
</pre>
