<%
// Inserts a link to a XPCOM Interface
// Appropriate styling is applied.
//
// Parameters:
//
//  $0 - Interace name

var lang = env.locale;

var URL = "/" + lang + "/docs/XPCOM_Interface_Reference/" + $0;

switch(lang) {
  case 'de':
    URL = "/de/docs/XPCOM_Interface_Referenz/" + $0;
    break;
}
%>
<%- template("xref", [URL, $0, false]) %>
