
<pre class="script">
/* get the page language */

var u = uri.parts(Page.uri);
var lang = string.tolower(u.path[0]);
if (string.contains(lang, "project") || string.contains(lang, "Project")) {
  lang = string.substr(lang, 8);
}
/* fall back to page.language on a user page */
else if (string.StartsWith(lang, "user:")) {
 lang = page.language;
}

var source = "Source";
var view = " View";

switch(lang) {
  case 'de':
    surce = "Datei";
    view = "Ansehen";
    break;
  case 'es':
    source = "Código";
    view = " Ver en funcionamiento";
    break;
  case 'fr':
    source = "Source";
    view = " Voir";
    break;
  case 'ja':
    source = "ソース";
    view = " 表示'";
    break;
  case 'pl':
    source = "Źródła";
    view = "Nowość w ";
    break;
  case 'ru':
    source = "Исходный код";
    view = " View";
    break;
}
web.link('http://developer.mozilla.org/samples/xultu/examples/' + $0 + '.txt', source);
 
web.link('http://developer.mozilla.org/samples/xultu/examples/' + $0, view);
%>
