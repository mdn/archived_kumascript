
<p><span class="script"><br />
  var fileList = map.select(page.files, "string.compare($.value.name, args.name, true)");<br />
  if (#fileList > 0) {<br />
    var textFile = map.values(fileList)[0];<br />
    web.text(textFile.uri);<br />
  }<br />
</span></p>
