
<p><span class="script"><br />
  var found = false;<br />
  var allFiles = map.values(page.files);<br />
  foreach(var aFile in allFiles) {<br />
    if (string.compare("foo", "bar", true) == 0) {<br />
      found = true;<br />
      web.text(aFile.uri);<br />
      break;<br />
    }<br />
  }<br />
  if (found == false) {<br />
    web.html("<p>Unable to find file '" + args[0] + "'.</p>");<br />
  }<br />
</span></p>
