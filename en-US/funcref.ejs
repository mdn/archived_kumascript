<%
// Inserts a link to a function located at the top level of the site;
// these should really move to where they actually belong, but there you go.
//
// Parameters:
//
//  $0 - Function name

var lang = env.locale;

var URL = "/" + lang + "/docs/" + $0;
%>
<%- template("xref", [URL, $0]) %>
