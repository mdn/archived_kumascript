<%
// Creates a link to a specific file on the MDN sample server, given
// the name of the sample and the name (or path) of the file within the
// sample.
//
// Parameters:
//  $0  The name of the sample
//  $1  The name of the file within the sample
//  $2  The link text (optional)
//

// urlRoot is the link to the base directory under which all the samples
// live. THIS NEEDS TO BE UPDATED ONCE WE MOVE THE REPO UNDER THE "mozilla"
// ACCOUNT ON GITHUB!

var urlRoot = "https://github.com/a2sheppy/mdn-samples/tree/master/s/";

// Build the URL to the sample

var url = urlRoot + $0 + "/" + $1;
var text = $2 || $0 | "/" + $1;

var html = "<a href='" + url + "'>" + text + "</a>";
%>
<%-html%>
