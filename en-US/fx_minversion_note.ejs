<span lang='en' class='lang lang-en'><div style="border: 1px solid #818151; background-color: #FFFFE1; text-align: center; padding: 0px 10px 0px 10px; margin: 10px 0px 10px 0px;"><p style="margin: 4px 0px 4px 0px; font-weight: bold;">{{wiki.pageexists(mediawiki.localurl("en", 'Firefox ' + $0 + ' for developers')) ? web.html(mediawiki.internal('Firefox ' + $0 + ' for developers|Firefox ' + $0 + ' note', "en")) : web.html('Firefox ' + $0 + ' note')}}</p><p style="margin: 4px 0px 4px 0px;">{{web.html($1)}}</p></div>
<div class="noinclude">
<hr />
<p>One of the <a href="Project:en/Custom_Templates#Version_information_templates">Version information templates</a>. For example:
</p>
<pre class="eval"><span class="plain">{{fx_minversion_note|3|The <code>foobar()</code> method works differently in Firefox 3.}}</span>
</pre>
<p>results in:
{{template.Fx_minversion_note(3, "The <code>foobar()</code> method works differently in Firefox 3.")}}
</p><p>This template should be used when you need to insert a Firefox version-specific note into an article. For individual items in a list or a table use <a href="Template:Fx_minversion_inline">Template:fx minversion inline</a>, and to mark a whole page or section, use <a href="Template:Fx_minversion_header">Template:fx_minversion_header</a>.
</p>
</div>
</span><span lang='*' class='lang lang-*'><div style="border: 1px solid #818151; background-color: #FFFFE1; text-align: center; padding: 0px 10px 0px 10px; margin: 10px 0px 10px 0px;"><p style="margin: 4px 0px 4px 0px; font-weight: bold;">{{wiki.pageexists(mediawiki.localurl("en", 'Firefox ' + $0 + ' for developers')) ? web.html(mediawiki.internal('Firefox ' + $0 + ' for developers|Firefox ' + $0 + ' note', "en")) : web.html('Firefox ' + $0 + ' note')}}</p><p style="margin: 4px 0px 4px 0px;">{{web.html($1)}}</p></div>
<div class="noinclude">
<hr />
<p>One of the <a href="Project:en/Custom_Templates#Version_information_templates">Version information templates</a>. For example:
</p>
<pre class="eval"><span class="plain">{{fx_minversion_note|3|The <code>foobar()</code> method works differently in Firefox 3.}}</span>
</pre>
<p>results in:
{{template.Fx_minversion_note(3, "The <code>foobar()</code> method works differently in Firefox 3.")}}
</p><p>This template should be used when you need to insert a Firefox version-specific note into an article. For individual items in a list or a table use <a href="Template:Fx_minversion_inline">Template:fx minversion inline</a>, and to mark a whole page or section, use <a href="Template:Fx_minversion_header">Template:fx_minversion_header</a>.
</p>
</div>
</span><span lang='es' class='lang lang-es'><div style="border: 1px solid #818151; background-color: #FFFFE1; text-align: center; padding: 0px 10px 0px 10px; margin: 10px 0px 10px 0px;"><p style="margin: 4px 0px 4px 0px; font-weight: bold;">{{wiki.pageexists(mediawiki.localurl("es", 'Firefox ' + $0 + ' para desarrolladores')) ? web.html('Nota sobre ' + mediawiki.internal('Firefox ' + $0 + ' para desarrolladores|Firefox ' + $0, "es")) : web.html('Nota sobre Firefox ' + $0)}}</p><p style="margin: 4px 0px 4px 0px;">{{web.html($1)}}</p></div>
<div class="noinclude">
<hr />
<p>For example:
</p>
<pre class="eval"><span class="plain">{{fx_minversion_note|3|The <code>foobar()</code> method works differently in Firefox 3.}}</span>
</pre>
<p>results in:
{{template.Fx_minversion_note(3, "The <code>foobar()</code> method works differently in Firefox 3.")}}
</p><p>This template should be used when you need to insert a Firefox version-specific note into an article. For individual items in a list or a table use <a href="Template:Fx_minversion_inline">Template:fx minversion inline</a>, and to mark a whole page or section, use <a href="Template:Fx_minversion_header">Template:fx_minversion_header</a>.
</p>
</div>
</span><span lang='fr' class='lang lang-fr'><div style="border: 1px solid #818151; background-color: #FFFFE1; text-align: center; padding: 0px 10px 0px 10px; margin: 10px 0px 10px 0px;"><p style="margin: 4px 0px 4px 0px; font-weight: bold;">{{wiki.pageexists(mediawiki.localurl("fr", 'Firefox ' + $0 + ' pour les développeurs')) ? web.html(mediawiki.internal('Firefox ' + $0 + ' pour les développeurs|Note concernant Firefox ' + $0, "fr")) : web.html('Note concernant Firefox ' + $0)}}</p><p style="margin: 4px 0px 4px 0px;">{{web.html($1)}}</p></div>
<div class="noinclude">
<hr />
<p>Un des <a href="Project:fr/Mod%c3%a8les_personnalis%c3%a9s#Version_information_templates">modèles d'informations de version</a>. Par exemple :
</p>
<pre class="eval"><span class="plain">{{fx_minversion_note|3|La méthode <code>foobar()</code> fonctionne différemment dans Firefox 3.}}</span>
</pre>
<p>donnera :
{{template.Fx_minversion_note(3, "La méthode <code>foobar()</code> fonctionne différemment das Firefox 3.")}}
</p><p>This template should be used when you need to insert a Firefox version-specific note into an article. For individual items in a list or a table use <a href="Template:Fx_minversion_inline">Template:fx minversion inline</a>, and to mark a whole page or section, use <a href="Template:Fx_minversion_header">Template:fx_minversion_header</a>.
</p>
</div>
</span><span lang='ja' class='lang lang-ja'><div style="border: 1px solid #818151; background-color: #FFFFE1; text-align: center; padding: 0px 10px 0px 10px; margin: 10px 0px 10px 0px;"><p style="margin: 4px 0px 4px 0px; font-weight: bold;">{{wiki.pageexists(mediawiki.localurl("ja", 'Firefox ' + $0 + ' for developers')) ? web.html(mediawiki.internal('Firefox ' + $0 + ' for developers|Firefox ' + $0 + ' における注記', "ja")) : web.html('Firefox ' + $0 + ' における注記')}}</p><p style="margin: 4px 0px 4px 0px;">{{web.html($1)}}</p></div>
<div class="noinclude">
<hr />
<p><a href="Project:ja/Custom_Templates#Version_information_templates">バージョン情報テンプレート</a> のうちの一つです。例えば:
</p>
<pre class="eval"><span class="plain">{{fx_minversion_note|3|Firefox 3 では、<code>foobar()</code> メソッドは、異なる動作をします。}}</span>
</pre>
<p>とすると、以下のように表示されます。:
{{template.Fx_minversion_note(3, "Firefox 3 では、<code>foobar()</code> メソッドは、異なる動作をします。")}}
このテンプレートは、Firefox のバージョン固有の注記を記事に挿入したいときに使うべきです。リストやテーブルの項目には、<a href="Template:Fx_minversion_inline">Template:fx minversion inline</a> を使い、ページまたはセクション全体には、<a href="Template:Fx_minversion_header">Template:fx_minversion_header</a> を使ってください。
</p>
</div>
</span><span lang='ko' class='lang lang-ko'><div style="border: 1px solid #818151; background-color: #FFFFE1; text-align: center; padding: 0px 10px 0px 10px; margin: 10px 0px 10px 0px;"><p style="margin: 4px 0px 4px 0px; font-weight: bold;">{{wiki.pageexists(mediawiki.localurl("ko", 'Firefox ' + $0 + ' for developers')) ? web.html(mediawiki.internal('Firefox ' + $0 + ' for developers|Firefox ' + $0 + ' note', "ko")) : web.html('Firefox ' + $0 + ' note')}}</p><p style="margin: 4px 0px 4px 0px;">{{web.html($1)}}</p></div>
<div class="noinclude">
<hr />
<p>One of the <a href="Project:ko/Custom_Templates#Version_information_templates">Version information templates</a>. For example:
</p>
<pre class="eval"><span class="plain">{{fx_minversion_note|3|The <code>foobar()</code> method works differently in Firefox 3.}}</span>
</pre>
<p>results in:
{{template.Fx_minversion_note(3, "The <code>foobar()</code> method works differently in Firefox 3.")}}
</p><p>This template should be used when you need to insert a Firefox version-specific note into an article. For individual items in a list or a table use <a href="Template:Fx_minversion_inline">Template:fx minversion inline</a>, and to mark a whole page or section, use <a href="Template:Fx_minversion_header">Template:fx_minversion_header</a>.
</p>
</div>
</span><span lang='pl' class='lang lang-pl'><div style="border: 1px solid #818151; background-color: #FFFFE1; text-align: center; padding: 0px 10px 0px 10px; margin: 10px 0px 10px 0px;"><p style="margin: 4px 0px 4px 0px; font-weight: bold;">{{wiki.pageexists(mediawiki.localurl("pl", 'Firefox ' + $0 + ' dla programistów')) ? web.html(mediawiki.internal('Firefox ' + $0 + ' dla programistów|Uwaga dotycząca Firefoksa ' + $0, "pl")) : web.html('Uwaga dotycząca Firefoksa ' + $0)}}</p><p style="margin: 4px 0px 4px 0px;">{{web.html($1)}}</p></div>
<div class="noinclude">
<hr />
<p>Jeden z <a href="Project:pl/W%c5%82asne_szablony#Szablony_informuj.C4.85ce_o_wersji">szablonów informujących o wersji</a>. Przykładowo:
</p>
<pre class="eval"><span class="plain">{{fx_minversion_note|3|Metoda <code>foobar()</code> działa inaczej w Firefoksie 3.}}</span>
</pre>
<p>spowoduje wyświetlenie:
{{template.Fx_minversion_note(3, "Metoda <code>foobar()</code> działa inaczej w Firefoksie 3.")}}
</p><p>Szablon ten powinien zostać użyty, gdy zachodzi potrzeba wstawienia do artykułu uwagi specyficznej dla danej wersji Firefoksa. Dla pojedynczych elementów listy lub tabelki należy użyć <a href="Template:Fx_minversion_inline">Szablon:fx minversion inline</a>, a do oznaczenia całej strony lub jej części, należy zastosować <a href="Template:Fx_minversion_header">Szablon:fx_minversion_header</a>.
</p>
</div>
</span>
