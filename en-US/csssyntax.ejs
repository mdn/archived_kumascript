<% 
    // Load the whole page (wiki.page does cache the original page
    var $page = wiki.page("en-US/docs/CSS/CSS_values_syntax"); 
    
    var $re = new RegExp("<\\s*td\\s+id\\s*=\\s*"+'"syntax-'+$0+'"'+"[^>]*>(.*)<\\s*/\\s*td\\s*\\>","igm");
    var $result = $re.exec($page)[1];
    if (!$result) $result="error:"+"(<\s*code\s+id\s*=\s*"+'"syntax-'+$0+'"'+"[^>]*>)";

%>
<%- $result %>
