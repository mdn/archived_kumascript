
<p><span lang="en" class="lang lang-en">
<div class="onlyinclude">
<div class="includeonly"><small style="margin-left: 1em;"><em>For <span class="script">string.tolower($1)</span> inherited by <code><span class="script">web.html($0)</span></code> instances, see <span class="script">mediawiki.internal('Core JavaScript 1.5 Reference:Global Objects:' + $0 + '#' + string.toupperfirst(string.tolower($1)) + '_2|' + string.toupperfirst(string.tolower($1)) + ' of ' + $0 + ' instances', "en")</span>.</em></small></div>
</div>
<h3 name="Syntax">Syntax</h3>
<p><code><span class="nowiki">{{</span><span class="script">page.unprefixedpath</span> | <em>constructor</em> | Properties<span class="nowiki">}}</span></code><br />
<code><span class="nowiki">{{</span><span class="script">page.unprefixedpath</span> | <em>constructor</em> | Methods<span class="nowiki">}}</span></code></p>
<h3 name="Parameters">Parameters</h3>
<dl><dt> constructor </dt><dd> JavaScript constructor that is the subject of the current page </dd></dl>
<p>The object named by the constructor parameter is case sensitive, although the second parameter is not.</p>
<h3 name="Description">Description</h3>
<p>Should be used to provide a helpful pointer to the included property/method list of the subject's prototype object. Place below the appropriate heading.</p>
</span><span lang="*" class="lang lang-*">
<div class="onlyinclude">
<div class="includeonly"><small style="margin-left: 1em;"><em>For <span class="script">string.tolower($1)</span> inherited by <code><span class="script">web.html($0)</span></code> instances, see <span class="script">mediawiki.internal('Core JavaScript 1.5 Reference:Global Objects:' + $0 + '#' + string.toupperfirst(string.tolower($1)) + '_2|' + string.toupperfirst(string.tolower($1)) + ' of ' + $0 + ' instances', "en")</span>.</em></small></div>
</div>
<h3 name="Syntax">Syntax</h3>
<p><code><span class="nowiki">{{</span><span class="script">page.unprefixedpath</span> | <em>constructor</em> | Properties<span class="nowiki">}}</span></code><br />
<code><span class="nowiki">{{</span><span class="script">page.unprefixedpath</span> | <em>constructor</em> | Methods<span class="nowiki">}}</span></code></p>
<h3 name="Parameters">Parameters</h3>
<dl><dt> constructor </dt><dd> JavaScript constructor that is the subject of the current page </dd></dl>
<p>The object named by the constructor parameter is case sensitive, although the second parameter is not.</p>
<h3 name="Description">Description</h3>
<p>Should be used to provide a helpful pointer to the included property/method list of the subject's prototype object. Place below the appropriate heading.</p>
</span><span lang="fr" class="lang lang-fr">
<div class="onlyinclude">
<div class="includeonly"><small style="margin-left: 1em;"><em>Pour les <span class="script">string.tolower($1)</span> héritées par les instances de <code><span class="script">web.html($0)</span></code> consultez <span class="script">mediawiki.internal('Référence de JavaScript 1.5 Core:Objets globaux:' + $0 + '#' + string.toupperfirst(string.tolower($1)) + '_2|' + string.toupperfirst(string.tolower($1)) + ' des instances de ' + $0, "fr")</span>.</em></small></div>
</div>
<h3 name="Syntaxe">Syntaxe</h3>
<p><code><span class="nowiki">{{</span><span class="script">page.unprefixedpath</span> | <em>constructeur</em> | Propriétés<span class="nowiki">}}</span></code><br />
<code><span class="nowiki">{{</span><span class="script">page.unprefixedpath</span> | <em>constructeur</em> | Méthodes<span class="nowiki">}}</span></code></p>
<h3 name="Param.C3.A8tres">Paramètres</h3>
<dl><dt> constructeur </dt><dd> Constructeur JavaScript sujet de la page courante </dd></dl>
<p>L'objet appelé par le paramètre du constructeur est sensible à la casse, même si le second paramètre ne l'est pas.</p>
<h3 name="Description">Description</h3>
<p>Doit être utilisé pour fournir un pointeur vers la liste des propriétés/méthodes de l'objet prototype du sujet. À placer en dessous du titre approprié.</p>
</span><span lang="ja" class="lang lang-ja">
<div class="onlyinclude"><small style="margin-left: 1em;"><em><code><span class="script">string.toupperfirst(string.tolower($0))</span></code> インスタンスから継承されている<span class="script">string.tolower($1)</span>については、<span class="script">mediawiki.internal('Core JavaScript 1.5 Reference:Global Objects:' + $0 + '#' + string.toupperfirst(string.tolower($1)) + '_2|' + string.toupperfirst(string.tolower($0)) + ' インスタンスの ' + string.toupperfirst(string.tolower($1)) + ' ', "ja")</span>を参照してください</em> </small></div>
<h3 name=".E6.A7.8B.E6.96.87">構文</h3>
<p><code><span class="nowiki">{{</span><span class="script">page.unprefixedpath</span> | <em>constructor</em> | プロパティ<span class="nowiki">}}</span></code><br />
<code><span class="nowiki">{{</span><span class="script">page.unprefixedpath</span> | <em>constructor</em> | メソッド<span class="nowiki">}}</span></code></p>
<p><span class="script">wiki.template('英語版章題', [ "Parameters" ])</span></p>
<h3 name=".E5.BC.95.E6.95.B0">引数</h3>
<dl><dt> constructor </dt><dd> 現在のページの題名と一致する JavaScript コンストラクタ </dd></dl>
<p><span class="script">wiki.template('英語版章題', [ "説明" ])</span></p>
<h3 name=".E8.AA.AC.E6.98.8E">説明</h3>
<p>そのページのプロトタイプオブジェクトのプロパティ/メソッドの一覧へ誘導するポインタを提供するために使われます。特定の冒頭の下に置いてください。</p>
<h3 name=".E6.97.A5.E6.9C.AC.E8.AA.9E.E7.89.88.E3.81.A7.E3.81.AE.E5.88.A9.E7.94.A8.E3.81.AB.E3.81.A4.E3.81.84.E3.81.A6.E3.81.AE.E6.B3.A8.E6.84.8F">日本語版での利用についての注意</h3>
<p>これは、取りこまれるページの章題が取り込むページにも存在することを前提としたテンプレートです。実際の章題は、取りこむページの TOC によって自動で調整されるため、特に問題はありませんが、英語版章題は、取り込まれるページの方で調整する必要があります。重複する英語版章題を noinclude とし、includeonly とした重複する英語版章題 2 を追加してください。そのとき、以下の例のように改行せずに続けて記述してください（無意味な br 要素が追加されるのを防ぐため）。</p>
<pre><noinclude>{{英語版章題|Methods}}</noinclude><includeonly>{{英語版章題|Methods 2}}</includeonly>
</pre>
<p>そうして調整した英語版章題がある場合、実際の章題を "プロパティ" と訳して、このテンプレートで "Properties" と指定しても問題は生じません。ただし、問題がないというだけの話であり、章題を "プロパティ" と訳す以上は、文書中での訳の整合性を保つという意味で、このテンプレートでも "プロパティ" と指定してください。</p>
</span><span lang="zh-cn" class="lang lang-zh-cn">
<div class="onlyinclude">
<div class="includeonly"><small style="margin-left: 1em;"><em>For <span class="script">string.tolower($1)</span> inherited by <code><span class="script">web.html($0)</span></code> instances, see <span class="script">mediawiki.internal('Core JavaScript 1.5 Reference:Global Objects:' + $0 + '#' + string.toupperfirst(string.tolower($1)) + '_2|' + string.toupperfirst(string.tolower($1)) + ' of ' + $0 + ' instances', "zh-cn")</span>.</em></small></div>
</div>
<h3 name="Syntax">Syntax</h3>
<p><code><span class="nowiki">{{</span><span class="script">page.unprefixedpath</span> | <em>constructor</em> | Properties<span class="nowiki">}}</span></code><br />
<code><span class="nowiki">{{</span><span class="script">page.unprefixedpath</span> | <em>constructor</em> | Methods<span class="nowiki">}}</span></code></p>
<h3 name="Parameters">Parameters</h3>
<dl><dt> constructor </dt><dd> JavaScript constructor that is the subject of the current page </dd></dl>
<p>The object named by the constructor parameter is case sensitive, although the second parameter is not.</p>
<h3 name="Description">Description</h3>
<p>Should be used to provide a helpful pointer to the included property/method list of the subject's prototype object. Place below the appropriate heading.</p>
</span></p>
