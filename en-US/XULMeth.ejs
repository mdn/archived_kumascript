<%
var lang = env.locale;

var anch = "m-" + $0;
var dispApi = ($2 || $0) + $1;

var methodName = mdn.localString({
  "en-US": "Method",
  "de": "Methoden",
  "fr": "Méthodes",
  "pl": "Metoda"
});

var destOldType = lang + '/docs/XUL:' + methodName + ':' + $0;
var destNewType = lang + '/docs/XUL/' + methodName + '/' + $0;

if (wiki.pageExists(destNewType)) {
  %><code><span><%-(web.link(wiki.uri(destNewType), dispApi))%></span></code><%
} else if (wiki.pageExists(destOldType)) {
  %><code><span><%-(web.link(wiki.uri(destOldType), dispApi))%></span></code><%
} else {
  %><code><span><%-(web.link(wiki.uri(destNewType), dispApi))%></span></code><%
}
%>
