<%
var lang = env.locale;
if(wiki.pageExists(lang + '/docs/CSS/' + $0)) {
  %><code><%-(web.link(wiki.uri(lang + '/docs/CSS/' + $0), ($1 || $0)))%></code><%
} else if (lang == 'zh-TW' && wiki.pageExists('en-US/docs/CSS/' + $0)){
  %><code><%-(web.link(wiki.uri('en-US/docs/CSS/' + $0), ($1 || $0)))%></code><%  
} else {
  var targeturi = "/'+lang+'/docs/new?slug=CSS/" + $0;
  %><code><a rel="internal" href="<%=targeturi%>" class="new"><%=($1 || $0)%></a></code><%
}
%>
