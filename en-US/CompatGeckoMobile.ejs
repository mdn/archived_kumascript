
<pre class="script">
var fxVer = '';
if (string.startswith($0, "1.") || !string.compare($0, "1")) {
  fxVer = '1.0';
} else {
  switch($0) {
    case '2':
    case '2.0':
      fxVer = '4.0';
      break;
  }
}
fxVer = fxVer + " (" + $0 + ")";
fxVer;
%>
