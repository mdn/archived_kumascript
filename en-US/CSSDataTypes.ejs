<%
/* Lists all the CSS data types, seperated by a comma */
/* Warning: special case for color (hardcoded) */
/* Non documented ones are not listed */

var data_types = ['angle', 'color', 'frequency', 'identifier', 'integer', 'length', 'number', 'percentage', 'resolution', 'shape', 'string', 'time', 'uri'];
var result;

web.text("start");

for (var i = 0;i<data_types.length;i++)
    web.text('in ForEach');
    if (result) {
      result += ', ';
    } 
    result += '<' + data_types[i] + '>'; 
;


web.text(result); 

</pre>
