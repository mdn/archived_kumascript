<%

/* one parameter: Gecko version as of which should only be used in mobile */
/* get the page language */

var u = uri.parts(Page.uri);
var lang = string.tolower(u.path[0]);
if (string.contains(lang, "project") || string.contains(lang, "Project")) {
  lang = string.substr(lang, 8);
}
/* fall back to page.language on a user page */
else if (string.StartsWith(lang, "user:")) {
 lang = page.language;
}

var str = "";
var title = "";

switch(lang) {
  case 'zh-cn':
    str = "只支持Gecko移动设备" + $0?("(Gecko" + $0 +"以上)"):"";
    title = "只在内核为Gecko " + $0 + " " + template.geckoRelease($0)"以上版本的Firefox Mobile中可用";
    break;
  default:
    str = "Mobile Only in Gecko " + $0;
    title = "Available only in Firefox Mobile as of Gecko " + $0 + " " + template.geckoRelease($0);
    break;
}

<span title=(title) style="border: 1px solid rgb(129, 129, 81); background-color: rgb(255, 225, 190); font-size: x-small; white-space: nowrap; padding: 0px 2px;">(web.text(str))</span>
</pre>
