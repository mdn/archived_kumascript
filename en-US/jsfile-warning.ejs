<span lang='en' class='lang lang-en'>{{template.Warning("This section describes the <code>File</code> component of the SpiderMonkey JavaScript interpreter. <code>File</code> is non-standard, not generally compiled into distributions, is a potential source of huge security holes, and not well tested.")}}
</span><span lang='*' class='lang lang-*'>{{template.Warning("This section describes the <code>File</code> component of the SpiderMonkey JavaScript interpreter. <code>File</code> is non-standard, not generally compiled into distributions, is a potential source of huge security holes, and not well tested.")}}
</span>
